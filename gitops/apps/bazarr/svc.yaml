apiVersion: v1
kind: Service
metadata:
  name: bazarr
  namespace: apps
spec:
  type: ClusterIP
  selector:
    app: bazarr
  ports:
    - name: http
      port: 6767
      targetPort: 6767
      protocol: TCP
