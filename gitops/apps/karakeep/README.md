Karakeep is wired here as a minimal single-container deployment.

Notes:
- This omits Meilisearch and a browser worker, so the app comes up with basic bookmark storage first.
- Search and richer crawling features can be added later once the base deployment is stable.
- Replace the placeholder value in `secret.yaml` before relying on the instance.
